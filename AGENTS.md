# Zooyanki CRM — backend

Мультимаркетплейс CRM: управление продажами на Avito, Ozon, Wildberries, Drom
из одного инструмента. Сейчас в работе — Авито.

Стек: NestJS, PostgreSQL + Prisma, BullMQ + Redis, TypeScript.
Фронтенд живёт в отдельном репозитории `zooyanki_crm` (Next.js), рядом в
`C:\Users\Илья\My projects\CRM\zooyanki_crm`.

## Перед работой прочитай

- `docs/03-roadmap.md` — текущая фаза и что в неё входит
- `docs/02-architecture.md` — слой адаптеров площадок и канонические модели
- `docs/04-avito-api.md` — карта API Авито, скоупы и его ограничения
- `docs/05-data-model.md` — схема данных и связки идентификаторов
- `docs/adr/` — принятые решения и их причины

## Инварианты

Их нарушение ломает главную цель проекта — подключать новые площадки без
переписывания ядра. Не нарушай без явной просьбы пользователя.

- Код, специфичный для площадки, живёт только в `src/channels/<channel>/`.
  Ни строки `avito` за пределами этой папки — ядро работает с каноническими
  моделями и интерфейсами из `src/channels/contracts/`.
- Каждая доменная таблица имеет `tenant_id`, доступ ограничен через Postgres RLS.
- Любой исходящий вызов к площадке идёт через очередь BullMQ с idempotency-ключом.
  Прямых HTTP-вызовов к площадкам из контроллеров быть не должно.
- Все запросы к площадкам проходят через общий HTTP-клиент с rate limiter
  и backoff — у Авито лимиты индивидуальны для каждого метода.
- Секреты площадок (`client_secret`, токены) хранятся только в зашифрованном виде,
  в логи не попадают.
- Клиенты к внешним API генерируются из их OpenAPI-схем, а не пишутся руками.
- Решения из `docs/adr/` не пересматриваются без явной просьбы.

## Рабочие соглашения

- Язык общения с пользователем — русский. Комментарии в коде и коммиты — русский.
- Схема БД и миграции живут в этом репозитории (`prisma/`), отдельного
  репозитория под базу нет — см. `docs/adr/0004-db-schema-in-backend-repo.md`.
- Обновляй `docs/03-roadmap.md` при завершении задач фазы.
