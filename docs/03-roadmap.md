# План работ

Статусы: `[ ]` не начато, `[~]` в работе, `[x]` готово.

## Фаза 1 — фундамент и чтение Авито `[~]`

Цель: подключённый аккаунт Авито, импорт объявлений и статистики.
Только чтение, аккаунту ничего не грозит.

- `[ ]` Скелет NestJS, TypeScript, ESLint, Prettier
- `[ ]` `docker-compose` с PostgreSQL и Redis для локальной разработки
- `[ ]` Конфиг через zod, типизированный `ConfigService`
- `[ ]` Prisma: начальная схема, миграции, RLS-политики по `tenant_id`
- `[ ]` Модуль `tenancy`: Tenant, User, Membership, контекст арендатора
- `[ ]` `core/crypto`: шифрование секретов площадок AES-256-GCM
- `[ ]` `core/http`: клиент с retry, backoff, token bucket, `api_call_log`
- `[ ]` `channels/contracts`: интерфейсы адаптеров и канонические DTO
- `[ ]` Генерация типизированного клиента Авито из OpenAPI-схем
- `[ ]` `AvitoTokenService`: `POST /token`, кеш в Redis по `expires_in`
- `[ ]` Подключение аккаунта: сохранение креды, проверка `/core/v1/accounts/self`
- `[ ]` Импорт объявлений: `GET /core/v1/items` с постраничным обходом
- `[ ]` Ночная джоба статистики: `/stats/v2/.../items` и `/spendings`
- `[ ]` REST для фронта + Swagger-схема
- `[ ]` Экран списка объявлений и графиков показов/контактов (web-репозиторий)

## Фаза 2 — заказы и склад `[ ]`

- `[ ]` Канонические `Order` и `OrderItem`, таблица соответствия статусов
- `[ ]` Опрос `/order-management/1/orders`, курсор синхронизации
- `[ ]` Переходы статусов через `applyTransition`, трек-номера, ярлыки
- `[ ]` Возвраты: `acceptReturnOrder`
- `[ ]` Модуль `inventory`: остатки, движения, резервы
- `[ ]` Публикация остатков в `/stock-management/1/stocks` через outbox
- `[ ]` Механизм outbox с idempotency-ключами

Требует подключённой Авито Доставки на аккаунте.

## Фаза 3 — цены и продвижение `[ ]`

- `[ ]` Обновление цены через `/core/v1/items/{item_id}/update_price` (150 rpm)
- `[ ]` Применение услуг продвижения: `/core/v2/items/{itemId}/vas/`, пакеты
- `[ ]` Баланс и история операций, расходы в аналитике

## Фаза 4 — мессенджер `[ ]`

- `[ ]` Канонические `Conversation` и `Message`
- `[ ]` Импорт чатов: `/messenger/v2/.../chats`, `/messenger/v3/.../messages/`
- `[ ]` Отправка текста и изображений
- `[ ]` Вебхук `/messenger/v3/webhook` + публичный HTTPS-эндпоинт
- `[ ]` Единое окно переписки в UI

Требует платной подписки на профинструменты (условие действует с ноября 2025).

## Фаза 5 — автозагрузка и отзывы `[ ]`

- `[ ]` Генерация XML-фида из нашего каталога
- `[ ]` Публичная раздача фида по HTTPS
- `[ ]` Запуск выгрузки и разбор отчётов, ошибки по каждой позиции
- `[ ]` Связка `SKU <-> ad_id <-> avito_id`
- `[ ]` Отзывы: `/ratings/v1/reviews` и ответы на них

Самая сложная и рискованная фаза, поэтому последняя.

## Фаза 6 — вторая площадка `[ ]`

- `[ ]` Адаптер Ozon Seller API — проверка, что абстракция выдержала
- `[ ]` Синхронизация остатков между площадками из единого склада
- `[ ]` Сквозная аналитика по всем площадкам

## Вне фаз

- `[ ]` CI: линт, типы, тесты, миграции
- `[ ]` Деплой и публичный HTTPS-домен (нужен для фида и вебхука)
