services:
  postgres:
    image: postgres:17-alpine
    container_name: zooyanki-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: zooyanki
      POSTGRES_PASSWORD: zooyanki
      POSTGRES_DB: zooyanki_crm
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U zooyanki -d zooyanki_crm']
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: zooyanki-redis
    restart: unless-stopped
    command: ['redis-server', '--appendonly', 'yes']
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
  redis-data:
